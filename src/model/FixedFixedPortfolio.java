package model;

import java.util.List;

public class FixedFixedPortfolio extends AbstractFixedPortfolio {
  public FixedFixedPortfolio(String portfolioName, List<IstockModelNew> stocks) {
    super(portfolioName, stocks);
  }
}
